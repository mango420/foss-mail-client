import { Head } from "next/document";
import React from "react";

const settings = () => {
    return (
        <React.Fragment>

            {/* <Head>
                <title>Settings</title>
            </Head> */}
            <title>Settings</title>

            <h1>Settings</h1>
        </React.Fragment>
    );
}

export default settings;